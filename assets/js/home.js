(async()=>{try{const list=(await getTrainings()).filter(x=>x.featured);const el=document.querySelector("#featured-grid");el.innerHTML=list.length?list.map(x=>`<a class="card" href="/training.html?id=${encodeURIComponent(x.id)}">${x.image?`<img src="${esc(x.image)}" alt="">`:""}<div class="card-body"><p class="eyebrow">${esc(x.category||"Professional learning")}</p><h3>${esc(x.title)}</h3><div class="meta"><span>${esc(x.dateDisplay||x.date)}</span><span>${esc(x.location)}</span></div><p>${esc(x.description)}</p><strong>${x.bookingOpen===false?"View details":"Book your place"} →</strong></div></a>`).join(""):`<div class="empty">New opportunities will be added shortly.</div>`;}catch(e){document.querySelector("#featured-grid").innerHTML=`<div class="empty">Training opportunities could not be loaded.</div>`;}})();