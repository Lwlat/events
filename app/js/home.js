async function loadEvents(){const r=await fetch('data/events.json');const d=await r.json();const es=await Promise.all(d.events.map(async i=>await(await fetch(i.path||i.data)).json()));document.getElementById('event-grid').innerHTML=es.map(e=>{const p=e.category==='Professional Learning';const m=p&&e.eventLogo?`<img src="${esc(e.eventLogo)}" alt="${esc(e.title)}">`:`<span>${esc(e.category||'Event')}</span>`;return `<a class="v2-event-card ${p?'pl-card':''}" href="event.html?id=${encodeURIComponent(e.id)}"><div class="v2-event-media">${m}<small>${esc(e.category||'Event')}</small></div><div class="v2-event-body"><h3>${esc(e.title)}</h3><div class="v2-event-meta"><span>${esc(e.venue||'')}</span><span>${esc(e.date||'')}</span></div><strong>${p?'Book your place':'Find out more'} →</strong></div></a>`}).join('')}function esc(v){return String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}loadEvents();