let all=[];async function load(){const r=await fetch('data/trainings.json',{cache:'no-store'}),d=await r.json();all=d.trainings||[];const c=[...new Set(all.map(x=>x.category).filter(Boolean))].sort();document.getElementById('training-category').innerHTML='<option value="">All categories</option>'+c.map(x=>`<option>${e(x)}</option>`).join('');render(all)}function render(a){document.getElementById('training-grid').innerHTML=a.length?a.map(x=>`<a class="training-card" href="training.html?id=${encodeURIComponent(x.id)}"><div class="training-card-media">${x.image?`<img src="${e(x.image)}" alt="${e(x.title)}">`:'<span>EduHub</span>'}<small>${e(x.category||'Training')}</small></div><div class="training-card-body"><h3>${e(x.title)}</h3><div class="training-card-meta"><span>${e(x.dateDisplay||x.date||'')}</span><span>${e(x.startTime||'')}${x.endTime?'–'+e(x.endTime):''}</span><span>${e(x.location||x.delivery||'')}</span></div><p>${e(x.description||'')}</p><strong>${x.bookingOpen===false?'Booking closed':'Book your place'} →</strong></div></a>`).join(''):'<article class="training-card"><div class="training-card-body"><h3>No matching opportunities found.</h3></div></article>'}function filter(){const q=document.getElementById('training-search').value.toLowerCase(),c=document.getElementById('training-category').value;render(all.filter(x=>(!q||[x.title,x.description,x.audience,x.location].join(' ').toLowerCase().includes(q))&&(!c||x.category===c)))}function e(v){return String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}document.getElementById('training-search').addEventListener('input',filter);document.getElementById('training-category').addEventListener('change',filter);load();